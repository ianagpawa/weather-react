import React, { Component } from 'react';

export default class SearchBar extends Componen {
    render () {
        return (
            <form className='input-group'>
                <input />
            </form>
        );
    }
}
